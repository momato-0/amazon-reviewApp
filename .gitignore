node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# pnpm is the project's package manager; avoid accidentally committing
# lockfiles generated by other tools
package-lock.json
yarn.lock

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Local-only assets, not pushed to remote
/photo-AmazonVine/
public/*.svg
public/*.png
